import { EffectDefinition } from "../types";
export interface PlaceholderAuraOptions {
    radius: number;
    pulseSpeed: number;
    particleCount: number;
    colors: string[];
}
export declare const PlaceholderAuraDefinition: EffectDefinition<PlaceholderAuraOptions>;
